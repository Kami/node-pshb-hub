config = {
  'max_retry_count': 5, // How many times to try and retry the subscription and
                       // unsubscription verification
  'retry_interval': 6 (60 * 60) // Retry interval in seconds
};

exports.config = config;
